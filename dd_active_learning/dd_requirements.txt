# =============================================================================
# Pip requirements for the Deep Docking active-learning virtualenv
# =============================================================================
# Used by the setup wizard's venv path (clusters without conda, e.g. the
# Digital Research Alliance of Canada, which installs from a local wheelhouse
# of "+computecanada" wheels rather than PyPI).
#
# Version RANGES (not ==) so a wheelhouse can satisfy them, e.g. Alliance's
# numpy 1.23.2+computecanada satisfies "numpy<1.24".  Constraints match what the
# DD code needs:
#   * tensorflow.keras (Keras 2)      -> tensorflow < 2.14
#   * np.bool (removed in numpy 1.24) -> numpy < 1.24
#   * pandas Series.append (gone 2.0) -> pandas < 2.0
#
# CUDA is NOT bundled here (no tensorflow[and-cuda]); it comes from the cuda
# module that is already loaded for the docking engine (gnina / AutoDock-GPU).
#
# Tip (Alliance): check what the wheelhouse actually has with
#   avail_wheels numpy pandas "tensorflow<2.14" rdkit meeko scikit-learn
# =============================================================================
numpy<1.24
pandas<2.0
scipy
matplotlib
scikit-learn
pyyaml
tqdm
rdkit
tensorflow>=2.8,<2.14
meeko>=0.5
